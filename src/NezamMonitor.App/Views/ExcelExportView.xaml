<UserControl x:Class="NezamMonitor.App.Views.ExcelExportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📤 خروجی اکسل" FontSize="{DynamicResource HeadingFontSize}" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="📊 ساخت فایل اکسل" Command="{Binding ExportCommand}"
                    Width="160" Height="35" Background="#27AE60" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"
                    FontSize="{DynamicResource ButtonFontSize}"/>
            <Button Content="📁 باز کردن پوشه" Command="{Binding OpenFolderCommand}"
                    Width="130" Height="35" Background="#3498DB" Foreground="White" Margin="0,0,10,0"
                    FontSize="{DynamicResource ButtonFontSize}"/>
            <Button Content="🔄 بروزرسانی" Command="{Binding RefreshCommand}"
                    Width="100" Height="35" Background="#95A5A6" Foreground="White"
                    FontSize="{DynamicResource ButtonFontSize}"/>
        </StackPanel>

        <ListBox Grid.Row="2" ItemsSource="{Binding ExistingFiles}" BorderThickness="1" BorderBrush="#BDC3C7"
                 FontSize="{DynamicResource FontSize}" Padding="8"/>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="#7F8C8D" FontSize="{DynamicResource LabelFontSize}" Margin="0,5,0,0"/>
    </Grid>
</UserControl>