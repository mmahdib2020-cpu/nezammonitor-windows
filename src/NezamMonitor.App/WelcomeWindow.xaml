<Window x:Class="NezamMonitor.App.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="خوش‌آمدگویی"
        Width="600" Height="350"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="White"
        AllowsTransparency="True"
        Opacity="0.95">
    <Border CornerRadius="12" BorderBrush="#1B4F72" BorderThickness="2" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1B4F72" CornerRadius="10,10,0,0" Padding="20,15">
                <TextBlock Text="🔄 نظارت مهندسی" 
                           Foreground="White" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </Border>

            <TextBlock Grid.Row="1"
                       Text="این نرم‌افزار توسط مهندس برخورداری به‌صورت اختصاصی برای مهندس مجتبی پورمروتی شریف‌آبادی تهیه و توسعه یافته است.&#x0a;&#x0a;هرگونه استفاده توسط سایر اشخاص، بدون کسب اجازه، قانوناً غیرمجاز و شرعاً حرام می‌باشد."
                       FontFamily="B Nazanin" FontSize="16" FontWeight="Bold"
                       TextAlignment="Center" VerticalAlignment="Center"
                       HorizontalAlignment="Center" TextWrapping="Wrap"
                       Margin="30,20" Foreground="#2C3E50"/>

            <Button Grid.Row="2" x:Name="CloseBtn" Content="بستن"
                    Width="120" Height="40" Background="#27AE60" Foreground="White"
                    FontSize="16" FontWeight="Bold" Cursor="Hand" Margin="0,0,0,20"
                    Click="CloseBtn_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
